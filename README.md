# Local File Indexer

A simple program to search your local files for keywords. 
- Load the files into a search index on program start
- Expose the index through simple HTTP requests
- Show a static webpage with a search bar
- Send the user's searches to the HTTP server and render the search results

Tools used:
- Go (backend language)
- Bleve (index package)
